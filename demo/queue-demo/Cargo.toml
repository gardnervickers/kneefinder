[package]
name = "kneefinder-queue-demo"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
kneefinder = { path = "../..", default-features = false }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
